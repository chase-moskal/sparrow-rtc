export declare function readInputs(): {
    flags: {
        dev: boolean;
        mini: boolean;
        host: string;
        lookup: string;
        root: string;
        semver: string;
    } & Record<string, unknown>;
    stdin: string;
};
