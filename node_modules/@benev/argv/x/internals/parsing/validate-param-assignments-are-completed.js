import { ArgvError } from "../../errors/argv-error.js";
export function validateParamAssignmentsAreCompleted(param) {
    if (param)
        throw new ArgvError(`missing value for param "${param}"`);
}
//# sourceMappingURL=validate-param-assignments-are-completed.js.map