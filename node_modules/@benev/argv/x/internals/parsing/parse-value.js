import { affirmatives } from "../constants.js";
export function parseValue(type, arg) {
    switch (type) {
        case String:
            return arg;
        case Number:
            return Number(arg);
        case Boolean:
            return affirmatives.includes(arg.toLowerCase());
        default:
            throw new Error("unknown type");
    }
}
//# sourceMappingURL=parse-value.js.map