import { FailedExpectation } from "./failed-expectation.js";
export const expect = (message) => ({
    that: (subject) => ({
        is: (expected) => {
            if (subject !== expected)
                throw new FailedExpectation(`${message}, expected "${expected}", got "${subject}"`);
        },
    }),
});
//# sourceMappingURL=expect.js.map