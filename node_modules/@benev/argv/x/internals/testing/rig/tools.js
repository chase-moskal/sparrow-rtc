import { stdargv } from "./stdargv.js";
export function tools(cli) {
    const run = (...a) => cli(stdargv(...a), { columns: 80 });
    const args = (...a) => run(...a).command.args;
    const params = (...a) => run(...a).command.params;
    const exitCode = (...a) => run(...a).exitCode;
    return { run, args, params, exitCode };
}
//# sourceMappingURL=tools.js.map