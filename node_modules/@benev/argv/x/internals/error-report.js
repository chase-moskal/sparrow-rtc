import { color } from "../tools/colors.js";
import { ArgvError } from "../errors/argv-error.js";
export function errorReport(err) {
    var _a, _b;
    const flag = color.red("ERR!!");
    return ((err instanceof Error)
        ? (err instanceof ArgvError)
            ? [
                flag + " " +
                    color.yellow(err.name) + " " +
                    color.green(err.message)
            ]
            : [
                flag + " " +
                    color.yellow(err.name) + " " +
                    color.green(err.message) + "\n" +
                    color.red((_b = ((_a = err
                        .stack) === null || _a === void 0 ? void 0 : _a.split("\n").slice(1).map(s => s.trim()).join("\n"))) !== null && _b !== void 0 ? _b : "")
            ]
        : [flag + " ", err]);
}
//# sourceMappingURL=error-report.js.map