import { repeat } from "./repeat.js";
export function indent(indenter, n, text) {
    return text
        .split("\n")
        .map(line => line.length > 0
        ? repeat(n, indenter) + line
        : line)
        .join("\n");
}
//# sourceMappingURL=indent.js.map