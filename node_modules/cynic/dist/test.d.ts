import { Suite } from "./types.js";
export declare function test(label: string, suite: Suite): Promise<{
    report: string;
    duration: number;
    total: number;
    failed: number;
    errors: Error[];
}>;
