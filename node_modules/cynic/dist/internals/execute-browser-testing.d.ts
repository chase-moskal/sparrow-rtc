export declare function executeBrowserTesting({ port, label, suitePath, cynicPath, importmapPath, }: {
    port: number;
    label: string;
    suitePath: string;
    cynicPath: string;
    importmapPath: string;
}): Promise<void>;
