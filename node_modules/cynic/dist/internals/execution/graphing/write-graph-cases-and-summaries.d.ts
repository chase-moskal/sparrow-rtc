import { Results } from "../execution-types.js";
export declare function writeGraphCasesAndSummaries({ depth, label, output, results, onlyShowErrors, }: {
    depth: number;
    label: string;
    output: string;
    results: Results;
    onlyShowErrors: boolean;
}): {
    caseReport: string;
    summaries: string[];
};
