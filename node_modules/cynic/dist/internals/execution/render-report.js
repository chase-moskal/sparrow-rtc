import { graph } from "./graph.js";
import { conclude } from "./conclude.js";
export function renderReport({ label, results, stats }) {
    const chart = graph(results);
    const conclusion = conclude(stats);
    let report = label;
    report += `\n${chart}`;
    report += `\n${conclusion}`;
    return report;
}
//# sourceMappingURL=render-report.js.map