import openUrl from "open";
import { cynicTestFileName } from "./constants.js";
import { runServer } from "./runners/run-server.js";
export async function executeBrowserTesting({ port, label, suitePath, cynicPath, importmapPath, }) {
    runServer({
        port,
        label,
        suitePath,
        cynicPath,
        importmapPath,
    });
    const url = `${origin}/${cynicTestFileName}`;
    console.log(`\n Test server running, see ${url}\n`);
    if (open)
        openUrl(url);
}
//# sourceMappingURL=execute-browser-testing.js.map