export function noop(strings, ...values) {
    function reducer(previous, current, index) {
        var _a;
        return previous + current + ((_a = values[index]) !== null && _a !== void 0 ? _a : "");
    }
    return Array
        .from(strings)
        .reduce(reducer, "");
}
//# sourceMappingURL=template-noop.js.map