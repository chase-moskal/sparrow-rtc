import { test } from "../../test.js";
export async function runNode(label, suite) {
    const { report, failed } = await test(label, suite);
    console.log(report);
    process.exit(failed === 0 ? 0 : 1);
}
//# sourceMappingURL=run-node.js.map