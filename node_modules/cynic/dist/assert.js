import { CynicBrokenAssertion } from "./errors.js";
export function assert(condition, fail = "failed assert") {
    if (!!condition)
        return true;
    else {
        const error = typeof fail === "string"
            ? new CynicBrokenAssertion(fail)
            : fail;
        throw error;
    }
}
//# sourceMappingURL=assert.js.map