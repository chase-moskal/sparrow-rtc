export function extractSessionInfo(session) {
    return {
        id: session.id,
        label: session.label,
        discoverable: session.discoverable,
    };
}
//# sourceMappingURL=extract-session-info.js.map